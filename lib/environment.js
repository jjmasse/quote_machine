// general configuration file

// handles app routing
// using iron-routes
Router.map(function() { 
  this.route('index', {path: '/'}); // main screen
  // this.route('hotel_landing'); // example route
});